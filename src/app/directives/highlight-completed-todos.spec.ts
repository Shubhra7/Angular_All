import { HighlightCompletedTodos } from './highlight-completed-todos';

describe('HighlightCompletedTodos', () => {
  it('should create an instance', () => {
    const directive = new HighlightCompletedTodos();
    expect(directive).toBeTruthy();
  });
});
